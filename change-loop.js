/**
 * Rewriting a While Loop as For Loop
 * Rewrite the following while loop as a for loop:
 * let x = 9;
while (x >= 1) {
  console.log("hello " + x);
  x = x - 1;
}
 */


for(let x = 9; x >= 1; x--){
    console.log("hello " + x);
}

